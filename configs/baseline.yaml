seed: 42
data_dir: data/thinning
ckpt_dir: checkpoints

batch_size: 4
num_workers: 0
epochs: 50
lr: 0.001
dice_weight: 0.5
val_ratio: 0.2
augment: true

# mask corruption (on-the-fly, input only; GT skeleton stays clean)
noise:
  intensity: 0.6
  seed: 42

# model
base_ch: 32
depth: 3

# device: auto (mps/cuda/cpu), or force e.g. "cpu"
device: null
